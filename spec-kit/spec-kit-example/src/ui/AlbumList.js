// src/ui/AlbumList.js
// Drag-and-drop UI for albums (skeleton)

export class AlbumList {
  constructor(container, albumService) {
    this.container = container;
    this.albumService = albumService;
    // TODO: Initialize drag-and-drop event listeners
  }

  render(albums) {
    // TODO: Render album list with drag-and-drop support
    throw new Error('Not implemented');
  }
}
