// src/ui/PhotoTiles.js
// Tile preview UI for photos (skeleton)

export class PhotoTiles {
  constructor(container, photoService) {
    this.container = container;
    this.photoService = photoService;
  }

  render(photos) {
    // TODO: Render photo tiles in the album
    throw new Error('Not implemented');
  }
}
